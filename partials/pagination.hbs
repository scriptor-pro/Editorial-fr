<footer>
    <nav class="pagination" role="navigation">
        {{#if prev}}
        <a class="page previous newer-posts" href="{{page_url prev}}">Plus récent</a>
        {{/if}}
        <span class="page-number">Page {{page}} sur un total de {{pages}}</span>
        {{#if next}}
        <a class="page next older-posts" href="{{page_url next}}">Plus ancien</a>
        {{/if}}
    </nav>
</footer>